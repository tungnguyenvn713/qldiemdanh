<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nor.qldiemdanh">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".AppContext"
        android:allowBackup="true"
        android:icon="@mipmap/imageutt2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.login.LoginActivity"
            android:theme="@style/AppTheme.NoActionbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.main.MainActivity"
            android:theme="@style/AppTheme.NoActionbar" />

        <activity
            android:name=".ui.teacher.TeacherActivity"
            android:label="@string/teacher" />

        <activity
            android:name=".ui.main.UserActivity"
            android:label="@string/update_profile" />

        <activity
            android:name=".ui.student.StudentActivity"
            android:label="@string/student" />
        <activity
            android:name=".ui.subject.SubjectActivity"
            android:label="@string/subject" />
        <activity
            android:name=".ui.schedule.ScheduleActivity"
            android:label="@string/schedule" />
        <activity
            android:name=".ui.room.RoomActivity"
            android:label="@string/room" />
        <activity
            android:name=".ui.schedule.ScheduleDetailActivity"
            android:label="@string/schedule_detail" />
        <activity
            android:name=".ui.main.QRCodeReaderActivity"
            android:label="@string/scan_qr_code" />
        <activity android:name=".ui.schedule.checkin.CheckInActivity" />
        <activity android:name=".ui.schedule.QRCodeViewer" />
    </application>

</manifest>